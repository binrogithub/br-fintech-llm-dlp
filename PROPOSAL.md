# 巴西 Fintech LLM 敏感数据防护（DLP）方案建议书

> 面向客户的实施建议书。技术工作语言为中文，最终用户可见文案（拦截/脱敏提示）
> 已采用葡语原文，可整体转译为 PT-BR / EN 用于客户正式交付。
> 配套材料：`DEMO_SCRIPT.md`（演示脚本）、`README.md`（技术细节）。

---

## 1. 背景与问题

员工与业务系统接入大模型（编码助手、客服、内部问答）已成常态。随之而来的新型
数据泄露通道是：**敏感数据被无意贴进 LLM 提示词**，绕过了客户现有的网络/终端 DLP
（它们从不检查"发往 LLM 的请求体"这条通道）。对受 **LGPD（巴西通用数据保护法）**
与央行（BACEN）监管的金融客户，CPF、银行卡、PIX、Boleto、口令等一旦外发不可逆。

**目标**：在 LLM 调用的出入口建立一道**由客户自己掌控**的实时管控，对敏感信息做到
**识别 → 脱敏/拦截 → 显性提示 → 合规审计**，且不打扰正常业务使用。

---

## 2. 方案概述（价值主张）

在 **客户自有的 LiteLLM 网关** 层插入一组 Guardrail，对每一次 LLM 请求在**调用上游
模型之前**完成敏感数据处理。三档动作:

| 动作 | 适用数据 | 效果 |
|---|---|---|
| **放行** | 正常业务文本 | 零打扰，无感知 |
| **脱敏 (MASK)** | CPF / CNPJ / PIX / 电话 / 姓名 / 地址 / 邮箱 | 替换为占位符后继续调用模型；**向用户显性提示**已脱敏 |
| **拦截 (BLOCK)** | 银行卡 / Boleto / 口令/CVV / API 密钥 / 私钥 | 直接拒绝，原文绝不外发任何模型 |

差异化亮点:
- **数据主权与隐私优先（核心）**:把控点放在客户自有网关，敏感数据在离开客户边界
  **之前**就被处理——原始数据**从不**到达任何云/模型厂商。这是依赖厂商内置措施在
  原理上做不到的（见第 3 节）。
- **巴西本地化识别**:CPF/CNPJ/信用卡/Boleto 均带**校验位算法**（mod-11 / Luhn /
  mod-10），辅以**葡语上下文加权**与 **Presidio 葡语 NER**（识别姓名/地址），显著
  压低误报。
- **显性化脱敏**:MASK 不是静默发生，用户会在回答前看到葡语隐私横幅，知情且可信。
- **可精准启用**:能力按入口开关（演示中 `claude-elo` 触发、普通入口不受影响），
  便于灰度与分人群推广。
- **统一管控、多模型一致**:策略在网关一处定义，更换上游模型（GLM / DeepSeek /
  Claude / 自有部署）策略不变，不随厂商而漂移。

---

## 3. 为什么把控点必须在自有网关，而非依赖云/模型厂商的内置敏感措施（核心论证）

许多云/模型厂商提供内置的"内容审核 / 安全过滤 / 厂商侧 DLP"。看似省事，但用于**敏感
数据防护**时存在一个**根本性矛盾**:

> **要被厂商的措施检查，原文必须先发送到厂商。** 也就是说，为了"防止敏感信息外发"，
> 你反而**先把它外发了**——数据已经进入厂商的边界、日志、乃至可能的训练管道。
> 这在原理上无法真正保护用户隐私。

**唯一能真正保护隐私的位置，是在数据离开客户边界之前** —— 即客户自有的 LiteLLM
网关。本方案在网关完成脱敏/拦截后，上游模型只会收到 `<BR_CPF>` 这样的占位符，
**真实敏感值从未离开客户掌控**。

### 自有网关把控 vs 依赖云/模型厂商措施

| 维度 | 依赖云/模型厂商内置措施 | 自有 LiteLLM 网关把控（本方案） |
|---|---|---|
| 原始敏感数据是否离开客户边界 | **是**——必须先把原文发给厂商才能被检查 | **否**——网关脱敏/拦截后才出边界 |
| 谁看到原文 | 云/模型厂商（及其日志，可能用于训练） | **仅客户自有网关** |
| 数据驻留（LGPD） | 取决于厂商，常不可控/跨境 | 客户境内自控（sa-east-1） |
| 策略控制权 | 厂商黑盒，客户改不了、看不全 | **客户白盒，策略即代码，可审计** |
| 多模型一致性 | 各厂商措施不一，换模型即变 | 统一在网关，换上游模型策略不变 |
| 失效模式 | 厂商可能 fail-open（静默放行） | 客户可强制 **fail-closed** |
| 审计证据 | 依赖厂商日志，原文可能被留存 | **自有结构化审计，永不留存原文** |
| 锁定风险 | 与厂商措施强绑定 | 与厂商解耦，可自由切换上游 |

**结论**:云/模型厂商的措施至多作为"最后一道冗余"。**第一道、也是决定性的一道，
必须落在客户自己掌控的网关上** —— 这是数据主权与用户隐私的根本要求。

---

## 4. 技术架构

```
用户/应用
   │  (Anthropic / OpenAI 兼容协议)
   ▼
客户自有 LiteLLM 网关  ── Guardrail 链（按序执行，全部 pre_call 前置）
   │
   ├─ L1 凭据硬规则     API key / JWT / 私钥 / AWS 密钥 → 命中即 BLOCK
   ├─ L2 巴西实体识别    正则 → 校验位 → 葡语上下文 → 策略（MASK/BLOCK）
   ├─ L2 Presidio NER   葡语姓名/地址（PERSON/LOCATION）语义识别 → MASK
   │
   ▼  脱敏/放行后的请求（仅含占位符）──► 上游模型（厂商只见 <BR_CPF>，不见原文）
   │
   └─ 输出侧扫描         模型回复回流他人敏感信息检查 + 追加隐私横幅
```

设计要点（合规硬约束）:
1. **必须 pre_call**:敏感数据过滤在调上游**之前**完成。绝不用 during_call（会与
   上游并行，等于原文已外发）。这是"自有网关把控"在请求层面的具体落实。
2. **不落原文日志**:审计记录仅含实体类型、动作、分数，`raw_content_logged=false`，
   永不记录命中的原始值——满足 LGPD 最小化原则。
3. **fail-closed**:扫描器自身异常时**拒绝**请求（金融默认），而非放行裸奔。
4. **分级路由**:客户端只见业务别名（public-ai / restricted-br），不直选供应商
   模型；含已脱敏个人信息的请求可路由至境内（sa-east-1）私有部署。

---

## 5. 识别能力覆盖

| 实体 | 方法 | 默认动作 |
|---|---|---|
| BR_CPF（个人税号） | 正则 + mod-11 校验位 + 上下文 | MASK |
| BR_CNPJ（企业税号，含 2026 字母数字新格式） | 正则 + 校验位 | MASK |
| CREDIT_CARD | Luhn 校验 + 卡品牌上下文 | BLOCK |
| BR_BOLETO（47 位 linha digitável） | 3×mod-10 字段校验 | BLOCK |
| BR_PIX_KEY（CPF/邮箱/电话/随机 EVP 键） | 多形态 + 上下文 | MASK |
| AUTH_SECRET（senha/PIN/OTP/CVV/CVC） | 关键词 + 分隔符 + 值形态评分 | BLOCK |
| BR_PHONE / BR_CEP / EMAIL | 正则 + 上下文 | MASK |
| PERSON / LOCATION（姓名/地址） | Presidio 葡语 NER | MASK |
| API key / JWT / 私钥 / AWS / GitHub / Slack | 内置内容过滤 | BLOCK |

> 阈值、动作（MASK/BLOCK）、葡语提示文案均在 `br_dlp_policy.yaml` 运行时可配，
> 改后重启网关生效，无需改代码。**全部规则与词典由客户掌握，非厂商黑盒。**

---

## 6. （可选）复用客户自有 DLP 的策略大脑——但执行始终留在网关

若客户内部已有企业 DLP（沉淀了分类、词典、数据指纹 EDM/IDM），可将其策略大脑接入，
**但要严格区分于"依赖云/模型厂商措施"**:

- 客户自有 DLP 在**客户信任边界内**，把原文送它鉴别是可接受的；
- 关键是：**脱敏/拦截的执行点仍在网关，数据在发往上游模型前已处理，原文绝不出客户
  边界到云/模型厂商**。

采用 **PDP / PEP 分离**:客户自有 DLP = 策略大脑（PDP），网关 Guardrail = 执行关卡
（PEP）。以适配器 `EnterpriseDLPGuardrail` 实现，仅需对接客户 DLP 的检测 API（一个
方法的映射），支持三种模式、建议分阶段灰度:

| 阶段 | 模式 | 行为 | 风险 |
|---|---|---|---|
| 一、观察 | **shadow（影子）** | 同步调 DLP 但不执行，只记录"DLP 判定 vs 本地判定"差异 | 零线上影响 |
| 二、审计 | **sidecar（旁路）** | 异步把副本送 DLP 审计/SIEM，不阻塞、不加延迟 | 零阻断风险 |
| 三、强制 | **inline（串联）** | 同步调 DLP 并执行脱敏/拦截，DLP 判定为权威 | 引入 DLP 延迟，需 fail 策略 |

**出错降级**可配:`block`（DLP 挂则拒绝，fail-closed）或 `passthrough`（DLP 挂则
降级到网关本地层继续兜底，不被 DLP 可用性绑死）。

> 注意:这一节是"复用客户**自有**资产"的可选增强，与第 3 节"不依赖云/模型厂商措施"
> 并不矛盾——前者在客户边界内、后者把数据交给外部厂商，本质不同。即便不接任何外部
> DLP，本方案在网关本地已是完整闭环。

---

## 7. 部署形态与落地步骤

**现状（PoC 已就绪）**:LiteLLM 网关容器化部署，Guardrail 以挂载文件注入，Presidio
葡语分析/匿名服务以 sidecar 容器叠加。已通过 33 项单测 + 25 项端到端测试 + 真实
链路全场景演示验证。

**客户侧实施步骤**:
1. **环境**:在客户境内（sa-east-1）部署 LiteLLM 网关 + Presidio 葡语服务（容器）。
   全部组件运行在客户自有基础设施，数据不出边界。
2. **接入**:业务/编码助手将 LLM 流量指向网关（兼容 OpenAI / Anthropic 协议，
   改 base_url 即可）。
3. **策略**:导入 `br_dlp_policy.yaml`，与客户合规团队确认各实体的 MASK/BLOCK 档位
   与葡语文案。
4. **（可选）DLP 对接**:如复用自有 DLP，按第 6 节 shadow → sidecar → inline 灰度。
5. **审计对接**:Guardrail 的结构化审计日志（仅实体类型）接入客户 SIEM。
6. **路由**:按敏感级别配置 public-ai / restricted-br 分级路由至对应模型部署。
7. **灰度推广**:先小范围入口启用，验证误报率后逐步扩面。

---

## 8. 安全与合规（LGPD 对齐）

- **数据主权**:管控落在客户自有网关，原始敏感数据从不到达云/模型厂商（第 3 节）。
- **数据最小化**:审计永不记录原文/命中值，仅实体类型 + 动作 + 分数。
- **前置管控**:pre_call 保证原文不在判定完成前抢跑上游。
- **境内驻留**:全部组件与含个人数据的请求可强制驻留 sa-east-1 私有部署。
- **可逆性兜底**:凭据类一律 BLOCK，不可逆外发风险归零。
- **fail-closed**:管控组件异常默认拒绝，不降级为裸奔。
- **可审计/可解释**:每次动作可溯源（虚拟 key alias），策略文件即合规依据，客户自持。

---

## 9. 演示（claude-elo）

见 `DEMO_SCRIPT.md`，10 个场景全部经真实链路验证:正常放行 / CPF·CNPJ·PIX·姓名
脱敏+横幅 / 信用卡·密码·Boleto·AWS密钥 拦截 / 误报防护 / 多轮历史引导 /clear。
演示用 `claude-elo` 入口触发,普通 `claude` 入口作对照,直观体现"按入口精准启用"，
并强调脱敏后上游模型只见占位符、原文从未外发。

---

## 10. 边界与说明

- 当前 MASK 为占位符替换；如需**可逆 token 化**（脱敏值可受控还原），为后续选项。
- BLOCK 当前以 HTTP 400 + 葡语提示返回，在交互式 CLI 中呈现为带提示的报错（功能
  正确）；如需更友好的"软提示替代回答"可定制。
- 流式输出的**输出侧强制**需缓冲后裁决（不能收回已吐出的内容）；当前隐私横幅已支持
  流式，输出侧 DLP 强制如启用需做 buffer-then-release（已预留）。
- 误报率随客户真实语料调优;建议灰度阶段收集数据校准阈值与词典。

---

## 11. 路线图（可选增强）

1. **可逆 token 化**:金库式映射,授权场景可在客户边界内还原。
2. **OPA 策略引擎**:把动作决策外置为可治理的策略即代码（L3）。
3. **（可选）现网自有 DLP inline 强制对接**（第 6 节阶段三）。
4. **分级路由生产化**:restricted-br 指向 sa-east-1 私有部署。
5. **自助误报反馈闭环**:用户标记误报 → 回流调优词典/阈值。

---

### 一句话总结
**把敏感数据的把控点放在客户自己掌控的 LiteLLM 网关上——在数据离开你之前就识别、
脱敏或拦截，原文从不交给任何云/模型厂商。这是比依赖厂商内置措施更彻底的隐私保护。**
